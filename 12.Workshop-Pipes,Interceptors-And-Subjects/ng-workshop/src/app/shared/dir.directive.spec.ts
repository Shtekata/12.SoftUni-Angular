import { SubmitOnValidDirective } from './dir.directive';

describe('DirDirective', () => {
  it('should create an instance', () => {
    // const directive = new SubmitOnValidDirective();
    // expect(directive).toBeTruthy();
  });
});
